<h2>Categories</h2>

<ol>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category_products_path(category) do %>
        <%= category.title %>
        (<%= category.products.count %> books)
      <% end %>
    </li>
  <% end %>
</ol>
